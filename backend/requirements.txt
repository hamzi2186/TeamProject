# Calling Engine backend dependencies
# Aligned with shared foundation (feat/foundation-auth-docker) versions

fastapi==0.115.12
uvicorn[standard]==0.34.2

# Database
sqlalchemy[asyncio]==2.0.40
asyncpg==0.30.0
alembic==1.15.2

# Settings
pydantic-settings==2.9.1
pydantic[email]==2.11.3

# Auth (verify RS256 JWT from shared root platform)
PyJWT[crypto]==2.10.1

# HTTP client (TPI calls, no Vapi SDK — TPI owns that)
httpx==0.28.1

# Celery workers
celery[redis]==5.5.2
redis==5.2.1
